# Secretos OPCIONALES que el instalador puede pedir en el install.
# Nunca se escriben en este repo: solo en la config local de cada runtime.

# Context7 (MCP de documentación) — cada usuario conecta su propia cuenta (PRD D5).
# Déjalo vacío para configurarlo después.
CONTEXT7_API_KEY=
